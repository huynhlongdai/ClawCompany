import {AuthGate} from "../../../components/AuthGate";import {V17AppShell} from "../../../components/V17AppShell";import {WorkspaceCockpit} from "../../../components/WorkspaceCockpit";export default function Page(){return <AuthGate><V17AppShell title="AI Organization OS" subtitle="Công ty, phòng ban, nhân sự, agent, dự án và tri thức — dữ liệu thật từ API"><WorkspaceCockpit/></V17AppShell></AuthGate>}
